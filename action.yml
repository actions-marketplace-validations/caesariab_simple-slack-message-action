# action.yml
name: "Simple Slack Message"
description: "Send a simple slack message to a channel"
inputs:
  send-data:
    description: "data to send"
    required: true
    default: "Hello, World!"
  slack-hook-url:
    description: "slack hook url"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.send-data }
    - ${{ inputs.slack-hook-url }}
